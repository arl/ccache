package main

import (
	"fmt"

	"arl.go2/cache"
	"arl.go2/stack"
)

func stackExample() {
	s := make(stack.Stack(int), 0)
	s.Push(0)
	s.Push(1)
	s.Push(2)
	s.Pop()

	fmt.Println(s)
}

func cacheExample() {
	lru := cache.LRU(int, string){}
	c := cache.New(256, lru)

	fmt.Println(lru, c)
}

func main() {
	//	stackExample()
	cacheExample()
}
