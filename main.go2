package main

import (
	"fmt"

	"arl.go2/cache"
)

func main() {
	c := cache.New(int, string)()
	v, ok := c.GetIfPresent(1)
	fmt.Printf("c.GetIfPresent(1) -> %q, %v\n", v, ok)
	c.Put(1, "hello")
	v, ok = c.GetIfPresent(1)
	fmt.Printf("c.GetIfPresent(1) -> %q, %v\n", v, ok)
}
